{
  "id": "invoice_analytics",
  "name": "Invoice Analytics",
  "version": "1.0.0",
  "description": "Fetch invoice data, analyze currency & totals.",
  "jsonSchema": {
    "type": "object",
    "properties": {
      "apiUrl": { "type": "string" },
      "token": { "type": "string" }
    },
    "required": ["apiUrl", "token"]
  }
}
