# .env.docker - used by docker containers (use service hostnames)
DATABASE_URL=postgresql://whms:whmspass@postgres:5432/whms_dev
REDIS_URL=redis://whms_redis:6379


# App settings
PORT=4000
NODE_ENV=development

# Email (use Mailtrap sandbox or real SMTP)
EMAIL_PROVIDER=smtp
SMTP_HOST=sandbox.smtp.mailtrap.io
SMTP_PORT=587
SMTP_USER=a1db30f836f3ea
SMTP_PASS=cb3e0fe4465467
FROM_EMAIL=noreply@whms.local
FROM_NAME="WHMS Billing"
