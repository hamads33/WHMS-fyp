'use client';

import EmailBuilder from '@/components/email-builder/EmailBuilder';

export default function NewEmailTemplatePage() {
  return <EmailBuilder templateId={null} initialTemplate={null} />;
}
